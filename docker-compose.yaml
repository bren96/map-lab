version: '3'
services:
  nx-base:
    build:
      context: libs/nx-base
      dockerfile: dockerfile
    command: sleep infinity
  
  monorepo:
    build:
      context: .
      dockerfile: dockerfile
    command: sleep infinity
    working_dir: /app
    volumes:
      - .:/app
  
