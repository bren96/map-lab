services:
  nx-base:
    build:
      context: libs/nx-base
      dockerfile: dockerfile
    command: sleep infinity
  
  monorepo:
    build:
      context: .
      dockerfile: dockerfile
    volumes:
      - .:/app

  ui-kit:
    build:
      context: .
      dockerfile: libs/ui-kit/dockerfile
    ports:
      - "4400:4400"

